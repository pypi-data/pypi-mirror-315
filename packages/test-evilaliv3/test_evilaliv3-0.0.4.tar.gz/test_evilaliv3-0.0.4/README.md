# globaleaks_ephemeral_fs

This is a Python package for managing an ephemeral encrypted file system, built on top of FUSE.

## Installation

You can install the package via pip:
pip install globaleaks_ephemeral_fs
