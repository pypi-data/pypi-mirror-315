[build-system]
requires = ["setuptools>=61", "scikit-build-core>=0.5.1", "pybind11>=2.6"]
build-backend = "setuptools.build_meta"

[project]
name = "pygbs"
version = "0.4.0a2"
description = "Python bindings for the GBS library"
readme = "README.md"
license = {"file" ="LICENSE.LGPLv3"}
authors = [
    {name = "Sébstien Raymond", email = "sebastien@ssg-aero.com"}
]

classifiers = [

]
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "numpy"]


[tool.scikit-build]
source-dir = "python" 

[tool.setuptools.packages.find]
where = ["pygbs"]