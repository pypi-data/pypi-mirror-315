[project]
name = "chainthon"
version = "0.1.1"
description = "A Python library for building chatbots and conversational applications（eg. RAG applications）"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ruff>=0.8.2",
    "fastapi[standard]>=0.115.6",
    "uvicorn>=0.32.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
