[tool.poetry]
name = "agentopia"
version = "0.1.6"
description = ""
authors = ["Yash Agarwal <yash@nurturelabs.co>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.0"  # Core HTTP library
eth-account = "^0.13.4"  # Ethereum account utilities
web3 = "^7.5"  # Ethereum/Web3 library
pipe = "*"  # Functional programming tools
pydantic-settings = "^2.6"  # Config management
python-dateutil = "^2.9"  # Date utilities
orjson = "^3.10"  # Fast JSON parsing
httpx = "*"
pycryptodome = "^3.21.0"

[tool.poetry.dev-dependencies]
pytest = "^8.3"  # Testing framework
mkdocs = "^1.6"  # Documentation generator
openai = "^1.55"  # OpenAI API client
fastapi = "^0.110.3"


[tool.poetry.group.dev-dependencies.dependencies]
pytest = "^8.3.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
