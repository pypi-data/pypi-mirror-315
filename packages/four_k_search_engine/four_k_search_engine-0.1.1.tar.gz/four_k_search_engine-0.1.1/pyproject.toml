[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "four_k_search_engine"
authors = [{name = "Mark W Anderson", email = "nosrednakram@gmail.com"}]
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version"]
description = "Simple search engine for a list of dictionaries with search and filter."

[tool.flit.module]
name = "four_k_search_engine_nosrednakram"