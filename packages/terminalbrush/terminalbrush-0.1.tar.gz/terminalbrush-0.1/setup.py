from setuptools import setup, find_packages

setup(
    name='terminalbrush',  # The name of your module
    version='0.1',    # Initial version
    packages=find_packages(),
    install_requires=[
       'requests'
    ]
)
