[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "py_blk_bash_db"
description = "split large file into 1MB small files."
authors = [
    { name = "GGN_2015", email = "premierbob@qq.com" }
]
version = "0.0.1"
dependencies = [
]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"

[tool.setuptools]
packages = [
    "py_blk_hash_db"
]