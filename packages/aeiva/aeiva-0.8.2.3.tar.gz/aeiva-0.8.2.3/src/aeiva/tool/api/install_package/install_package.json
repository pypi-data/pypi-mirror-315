{
    "type": "function",
    "function": {
      "name": "install_package",
      "description": "Installs a specified system or Python package.",
      "parameters": {
        "type": "object",
        "properties": {
          "package_name": {
            "type": "string",
            "description": "The name of the package to install."
          },
          "package_type": {
            "type": "string",
            "description": "Type of package ('python' or 'system'). Defaults to 'python'.",
            "default": "python"
          }
        },
        "required": ["package_name"]
      }
    }
  }