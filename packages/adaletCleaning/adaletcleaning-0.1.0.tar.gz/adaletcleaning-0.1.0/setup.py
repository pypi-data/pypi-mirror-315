from setuptools import setup, find_packages

setup(

    name="adaletCleaning",
    version= "0.1.0",
    description="toplama fonk",
    packages=find_packages(include=[('adaletCleaning')])

)