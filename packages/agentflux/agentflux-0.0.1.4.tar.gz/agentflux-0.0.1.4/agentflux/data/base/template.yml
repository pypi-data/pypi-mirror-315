template:
  tool_template: "\
  \n{{TOOL_NAME}}
  \n  the tool is used for {{TOOL_DESCRIPTION}}.
  \n  the input parameters of the tool:{{ARGS_LIST}}
  \n  the return arguments of the tool:{{RETURN_LIST}}\
  \n"
  arg_template: "\
  \"{{ARG_NAME}}\":type shuld be {{ARG_TYPE}},{{ARG_DESCRIBE}}.\n\
  "