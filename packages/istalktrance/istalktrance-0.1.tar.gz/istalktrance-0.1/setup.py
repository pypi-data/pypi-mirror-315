from setuptools import setup, find_packages

setup(
    name="istalktrance",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
        'numpy>=1.11.1'
    ],
)