[project]
name = "magic-maths"
version = "0.1.1"
description = "magically import evaluated math expressions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = ["magic_math"]