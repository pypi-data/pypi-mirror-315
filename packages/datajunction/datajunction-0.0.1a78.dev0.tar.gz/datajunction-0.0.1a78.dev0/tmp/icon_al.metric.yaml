description: ''
display_name: Icon al
query: |-
  SELECT ln(count( DISTINCT CASE
          WHEN coalesce(app_launch_source_type, -1) IN (2, 28) THEN client_utc_epoch_ts
          ELSE NULL
      END) + 1) FROM ${prefix}app_launches_ab_annotated
