display_name: Homepage Qualified Take Rate
description: Homepage Qualified Take Rate
query: |-
  SELECT  1.0 * SUM(COALESCE(sum_pct_qualified_take_rate, 0)) / NULLIF(SUM(COALESCE(session_cnt, 0)), 0)
   FROM ${prefix}eclipse.tvui_homepage_account_counts_base
tags:
- xp.report
required_dimensions: []
direction: higher_is_better
unit: percentage
