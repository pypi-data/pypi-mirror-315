[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "karaoke_helper"
version = "0.1.0"
description = "This package is a collection of functions helpful for karaoke-ai-bot"
authors = ["Grumpy Chipmunk"]
packages = [
    { include = "karaoke_helper", from = "src" }
]