[tool.poetry]
name = "long_task_printer"
version = "0.1.2"
description = "Measure iterations in a long-running task, provide a time estimation"
authors = ["Andrew Radev <andrey.radev@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.group.test.dependencies]
pytest = "^8.3.2"
freezegun = "^1.5.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
