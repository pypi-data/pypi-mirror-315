"""Autogenerated API"""
from argus_api.session import _legacy_session as session
import logging
from argus_cli.plugin import register_command
from argus_plugins import argus_cli_module
from argus_api.utils import deprecated_alias
log = logging.getLogger(__name__)


@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def attach_artifact(
    profileID: int,
    artifactID: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Attach artifact to profile (INTERNAL)
    
    :param int profileID: ID of profile to attach to
    :param list artifactID: Hash ID of artifacts to attach
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{profileID}/artifacts/{artifactID}".format(profileID=profileID,
        artifactID=artifactID)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("POST %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.post(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def attach_artifact_by_name(
    profileName: str,
    groupID: str,
    artifactID: str,
    version: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Attach artifact to profile by profile name and artifact GAV (INTERNAL)
    
    :param str profileName: Name of profile to attach to
    :param str groupID: GroupID of artifact to attach
    :param str artifactID: ArtifactID of artifact to attach
    :param str version: Version of artifact to attach
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{profileName}/artifacts/{groupID}/{artifactID}/{version}".format(profileName=profileName,
        groupID=groupID,
        artifactID=artifactID,
        version=version)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("POST %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.post(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def attach_detach_artifacts(
    profileID: int,
    artifactsToAttach: str = None,
    artifactsToDetach: str = None,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Attach and detach artifact to given profile (INTERNAL)
    
    :param int profileID: ID of profile to detach from
    :param list artifactsToAttach: List of artifact SHA256 ids to attach
    :param list artifactsToDetach: List of artifact SHA256 ids to detach
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{profileID}/artifacts".format(profileID=profileID)
    headers = {}

    body = body or {}
    # Only send artifactsToAttach if the argument was provided, don't send null values
    if artifactsToAttach is not None:
        body.update({"artifactsToAttach": artifactsToAttach})
    # Only send artifactsToDetach if the argument was provided, don't send null values
    if artifactsToDetach is not None:
        body.update({"artifactsToDetach": artifactsToDetach})

    query_parameters = {}
    log.debug("PUT %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.put(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def create_code_profile(
    name: str = None,
    artifactsToAttach: str = None,
    finalize: bool = None,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Add new code profile (INTERNAL)
    
    :param str name: The name of the new code profile \=\> \[a\-zA\-Z0\-9\_\:\\\-\\.\]\*
    :param list artifactsToAttach: List of artifact SHA256 ids to attach to this profile
    :param bool finalize: If true\, finalize immediately upon creation. When finalized\, the profile cannot be modified anymore.
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile".format()
    headers = {}

    body = body or {}
    # Only send name if the argument was provided, don't send null values
    if name is not None:
        body.update({"name": name})
    # Only send artifactsToAttach if the argument was provided, don't send null values
    if artifactsToAttach is not None:
        body.update({"artifactsToAttach": artifactsToAttach})
    # Only send finalize if the argument was provided, don't send null values
    if finalize is not None:
        body.update({"finalize": finalize})

    query_parameters = {}
    log.debug("POST %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.post(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def delete_code_profile(
    id: int,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Mark code profile as deleted (INTERNAL)
    
    :param int id: ID of profile to delete
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{id}".format(id=id)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("DELETE %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.delete(
        route,
        params=query_parameters or None,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def detach_artifact(
    profileID: int,
    artifactID: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Detach artifact to profile (INTERNAL)
    
    :param int profileID: ID of profile to detach from
    :param str artifactID: Hash ID of artifact to detach
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{profileID}/artifacts/{artifactID}".format(profileID=profileID,
        artifactID=artifactID)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("DELETE %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.delete(
        route,
        params=query_parameters or None,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def detach_artifact_by_gav(
    profileName: str,
    groupID: str,
    artifactID: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Detach artifact from profile by profile name and artifact GAV (INTERNAL)
    
    :param str profileName: Name of profile to detach from
    :param str groupID: GroupID of artifact to detach
    :param str artifactID: ArtifactID of artifact to detach
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{profileName}/artifacts/{groupID}/{artifactID}".format(profileName=profileName,
        groupID=groupID,
        artifactID=artifactID)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("DELETE %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.delete(
        route,
        params=query_parameters or None,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def detach_artifact_by_name(
    profileName: str,
    groupID: str,
    artifactID: str,
    version: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Detach artifact from profile by profile name and artifact GAV (INTERNAL)
    
    :param str profileName: Name of profile to detach from
    :param str groupID: GroupID of artifact to detach
    :param str artifactID: ArtifactID of artifact to detach
    :param str version: Version of artifact to detach
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{profileName}/artifacts/{groupID}/{artifactID}/{version}".format(profileName=profileName,
        groupID=groupID,
        artifactID=artifactID,
        version=version)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("DELETE %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.delete(
        route,
        params=query_parameters or None,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def duplicate_code_profile(
    id: int,
    name: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Duplicate existing code profile (INTERNAL)
    
    :param int id: ID of profile to duplicate
    :param str name: Name of new profile
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{id}/duplicate/{name}".format(id=id,
        name=name)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("POST %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.post(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def get_code_profile_by_id(
    id: int,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Get code profile by ID (INTERNAL)
    
    :param int id: ID of profile to fetch
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{id}".format(id=id)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("GET %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.get(
        route,
        params=query_parameters or None,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def get_code_profile_by_name(
    name: str,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Get code profile by name (INTERNAL)
    
    :param str name: Name of profile to fetch
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{name}".format(name=name)
    headers = {}

    body = body or {}

    query_parameters = {}
    log.debug("GET %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.get(
        route,
        params=query_parameters or None,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def list_code_profiles(
    orderDesc: bool = None,
    limit: int = 25,
    orderBy: str = "name",
    offset: int = None,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """List code profiles (INTERNAL)
    
    :param bool orderDesc: Sort results descending
    :param int limit: Limit results
    :param str orderBy: Sort results
    :param int offset: Offset results
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile".format(limit=limit,
        orderBy=orderBy,
        offset=offset,
        orderDesc=orderDesc)
    headers = {}

    body = body or {}

    query_parameters = {}
    # Only send limit if the argument was provided, don't send null values
    if limit is not None:
        query_parameters.update({"limit": limit})
    
    # Only send orderBy if the argument was provided, don't send null values
    if orderBy is not None:
        query_parameters.update({"orderBy": orderBy})
    
    # Only send offset if the argument was provided, don't send null values
    if offset is not None:
        query_parameters.update({"offset": offset})
    
    # Only send orderDesc if the argument was provided, don't send null values
    if orderDesc is not None:
        query_parameters.update({"orderDesc": orderDesc})
    
    log.debug("GET %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.get(
        route,
        params=query_parameters or None,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def search_code_profiles(
    searchString: str = None,
    usingArtifact: str = None,
    includeArtifacts: bool = None,
    includeCreatedTimestamp: bool = None,
    includeLastUpdatedTimestamp: bool = None,
    limit: int = None,
    offset: int = None,
    includeDeleted: bool = None,
    subCriteria: dict = None,
    exclude: bool = None,
    required: bool = None,
    startTimestamp: str = None,
    endTimestamp: str = None,
    sortBy: str = None,
    includeFlags: str = None,
    excludeFlags: str = None,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Search code profiles (INTERNAL)
    
    :param str searchString: 
    :param list usingArtifact: 
    :param bool includeArtifacts: 
    :param bool includeCreatedTimestamp: 
    :param bool includeLastUpdatedTimestamp: 
    :param int limit: Max number of results.
    :param int offset: Skip the first \(offset\) objects. By default\, return result from first object.
    :param bool includeDeleted: Set to true to include deleted objects. By default\, exclude deleted objects.
    :param list subCriteria: Set additional criterias which are applied using a logical OR.
    :param bool exclude: Only relevant for subcriteria. If set to true\, objects matching this subcriteria object will be excluded.
    :param bool required: Only relevant for subcriteria. If set to true\, objects matching this subcriteria are required \(AND\-ed together with parent criteria\).
    :param str startTimestamp: Restrict search to data after this time \(epoch millis\) according to selected TimeFieldStrategies. Allows unix timestamp \(milliseconds\)\, ISO timestamp\, or relative time specifies. See https\:\/\/docs.mnemonic.no\/x\/AQDXAQ
    :param str endTimestamp: Restrict search to data before this time \(epoch millis\) according to selected TimeFieldStrategies. Allows unix timestamp \(milliseconds\)\, ISO timestamp\, or relative time specifies. See https\:\/\/docs.mnemonic.no\/x\/AQDXAQ
    :param list sortBy: List of properties to sort by \(prefix with \"\-\" to sort descending\).
    :param list includeFlags: Only include objects which have includeFlags set.
    :param list excludeFlags: Exclude objects which have excludeFlags set.
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/search".format()
    headers = {}

    body = body or {}
    # Only send searchString if the argument was provided, don't send null values
    if searchString is not None:
        body.update({"searchString": searchString})
    # Only send usingArtifact if the argument was provided, don't send null values
    if usingArtifact is not None:
        body.update({"usingArtifact": usingArtifact})
    # Only send includeArtifacts if the argument was provided, don't send null values
    if includeArtifacts is not None:
        body.update({"includeArtifacts": includeArtifacts})
    # Only send includeCreatedTimestamp if the argument was provided, don't send null values
    if includeCreatedTimestamp is not None:
        body.update({"includeCreatedTimestamp": includeCreatedTimestamp})
    # Only send includeLastUpdatedTimestamp if the argument was provided, don't send null values
    if includeLastUpdatedTimestamp is not None:
        body.update({"includeLastUpdatedTimestamp": includeLastUpdatedTimestamp})
    # Only send limit if the argument was provided, don't send null values
    if limit is not None:
        body.update({"limit": limit})
    # Only send offset if the argument was provided, don't send null values
    if offset is not None:
        body.update({"offset": offset})
    # Only send includeDeleted if the argument was provided, don't send null values
    if includeDeleted is not None:
        body.update({"includeDeleted": includeDeleted})
    # Only send subCriteria if the argument was provided, don't send null values
    if subCriteria is not None:
        body.update({"subCriteria": subCriteria})
    # Only send exclude if the argument was provided, don't send null values
    if exclude is not None:
        body.update({"exclude": exclude})
    # Only send required if the argument was provided, don't send null values
    if required is not None:
        body.update({"required": required})
    # Only send startTimestamp if the argument was provided, don't send null values
    if startTimestamp is not None:
        body.update({"startTimestamp": startTimestamp})
    # Only send endTimestamp if the argument was provided, don't send null values
    if endTimestamp is not None:
        body.update({"endTimestamp": endTimestamp})
    # Only send sortBy if the argument was provided, don't send null values
    if sortBy is not None:
        body.update({"sortBy": sortBy})
    # Only send includeFlags if the argument was provided, don't send null values
    if includeFlags is not None:
        body.update({"includeFlags": includeFlags})
    # Only send excludeFlags if the argument was provided, don't send null values
    if excludeFlags is not None:
        body.update({"excludeFlags": excludeFlags})

    query_parameters = {}
    log.debug("POST %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.post(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

@register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module
)
def update_code_profile(
    id: int,
    name: str = None,
    finalized: bool = None,
    useForUnknown: bool = None,
    setAsDefault: bool = None,
    json: bool = True,
    verify: bool = None,
    proxies: dict = None,
    apiKey: str = None,
    authentication: dict = {},
    server_url: str = None,
    body: dict = None,
) -> dict:
    """Update code profile (INTERNAL)
    
    :param int id: ID of profile to update
    :param str name: New name of code profile. \=\> \[a\-zA\-Z0\-9\_\:\\\-\\.\]\*
    :param bool finalized: If true\, finalize this code profile. Once finalized\, the profile cannot be modified anymore.
    :param bool useForUnknown: DEPRECATED\: Not in use
    :param bool setAsDefault: DEPRECATED\: Not in use
    :param json: return the response's body as a ``dict`` parsed from json. ``True`` by
      default. If set to false, the raw ``requests.Response`` object will be returned.
    :param verify: path to a certificate bundle or boolean indicating whether SSL
      verification should be performed.
    :param apiKey: Argus API key.
    :param authentication: authentication override
    :param server_url: API base URL override
    :param body: body of the request. other parameters will override keys defined in the body.
    :raises AuthenticationFailedException: on 401
    :raises AccessDeniedException: on 403
    :raises ObjectNotFoundException: on 404
    :raises ValidationErrorException: on 412
    :raises ArgusException: on other status codes
    
    :returns: dictionary translated from JSON
    """

    route = "/configurationadmin/v1/profile/{id}".format(id=id)
    headers = {}

    body = body or {}
    # Only send name if the argument was provided, don't send null values
    if name is not None:
        body.update({"name": name})
    # Only send finalized if the argument was provided, don't send null values
    if finalized is not None:
        body.update({"finalized": finalized})
    # Only send useForUnknown if the argument was provided, don't send null values
    if useForUnknown is not None:
        body.update({"useForUnknown": useForUnknown})
    # Only send setAsDefault if the argument was provided, don't send null values
    if setAsDefault is not None:
        body.update({"setAsDefault": setAsDefault})

    query_parameters = {}
    log.debug("PUT %s (headers: %s, body: %s)" % (route, str(headers), str(body) or ""))

    response = session.put(
        route,
        params=query_parameters or None,
        json=body,
        verify=verify,
        apiKey=apiKey,
        authentication=authentication,
        server_url=server_url,
        headers=headers,
        proxies=proxies,
    )
    return response.json() if json else response

#: **DEPRECATED** : ``detach_artifact_by_g_a`` is an alias for ``detach_artifact_by_gav``. Exists
#: only for backward compatibility - **do not use** - use ``detach_artifact_by_gav`` instead.
detach_artifact_by_g_a = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="detach_artifact_by_g_a"
)(deprecated_alias("detach_artifact_by_g_a")(detach_artifact_by_gav))
#: **DEPRECATED** : ``attach`` is an alias for ``attach_artifact``. Exists
#: only for backward compatibility - **do not use** - use ``attach_artifact`` instead.
attach = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="attach"
)(deprecated_alias("attach")(attach_artifact))
#: **DEPRECATED** : ``attach_by_name`` is an alias for ``attach_artifact_by_name``. Exists
#: only for backward compatibility - **do not use** - use ``attach_artifact_by_name`` instead.
attach_by_name = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="attach_by_name"
)(deprecated_alias("attach_by_name")(attach_artifact_by_name))
#: **DEPRECATED** : ``attach_detach`` is an alias for ``attach_detach_artifacts``. Exists
#: only for backward compatibility - **do not use** - use ``attach_detach_artifacts`` instead.
attach_detach = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="attach_detach"
)(deprecated_alias("attach_detach")(attach_detach_artifacts))
#: **DEPRECATED** : ``create_1`` is an alias for ``create_code_profile``. Exists
#: only for backward compatibility - **do not use** - use ``create_code_profile`` instead.
create_1 = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="create_1"
)(deprecated_alias("create_1")(create_code_profile))
#: **DEPRECATED** : ``delete`` is an alias for ``delete_code_profile``. Exists
#: only for backward compatibility - **do not use** - use ``delete_code_profile`` instead.
delete = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="delete"
)(deprecated_alias("delete")(delete_code_profile))
#: **DEPRECATED** : ``detach`` is an alias for ``detach_artifact``. Exists
#: only for backward compatibility - **do not use** - use ``detach_artifact`` instead.
detach = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="detach"
)(deprecated_alias("detach")(detach_artifact))
#: **DEPRECATED** : ``detach_by_artifact_by_g_a`` is an alias for ``detach_artifact_by_gav``. Exists
#: only for backward compatibility - **do not use** - use ``detach_artifact_by_gav`` instead.
detach_by_artifact_by_g_a = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="detach_by_artifact_by_g_a"
)(deprecated_alias("detach_by_artifact_by_g_a")(detach_artifact_by_gav))
#: **DEPRECATED** : ``detach_by_name`` is an alias for ``detach_artifact_by_name``. Exists
#: only for backward compatibility - **do not use** - use ``detach_artifact_by_name`` instead.
detach_by_name = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="detach_by_name"
)(deprecated_alias("detach_by_name")(detach_artifact_by_name))
#: **DEPRECATED** : ``duplicate`` is an alias for ``duplicate_code_profile``. Exists
#: only for backward compatibility - **do not use** - use ``duplicate_code_profile`` instead.
duplicate = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="duplicate"
)(deprecated_alias("duplicate")(duplicate_code_profile))
#: **DEPRECATED** : ``get_by_id_1`` is an alias for ``get_code_profile_by_id``. Exists
#: only for backward compatibility - **do not use** - use ``get_code_profile_by_id`` instead.
get_by_id_1 = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="get_by_id_1"
)(deprecated_alias("get_by_id_1")(get_code_profile_by_id))
#: **DEPRECATED** : ``get_by_name`` is an alias for ``get_code_profile_by_name``. Exists
#: only for backward compatibility - **do not use** - use ``get_code_profile_by_name`` instead.
get_by_name = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="get_by_name"
)(deprecated_alias("get_by_name")(get_code_profile_by_name))
#: **DEPRECATED** : ``list_1`` is an alias for ``list_code_profiles``. Exists
#: only for backward compatibility - **do not use** - use ``list_code_profiles`` instead.
list_1 = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="list_1"
)(deprecated_alias("list_1")(list_code_profiles))
#: **DEPRECATED** : ``search_1`` is an alias for ``search_code_profiles``. Exists
#: only for backward compatibility - **do not use** - use ``search_code_profiles`` instead.
search_1 = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="search_1"
)(deprecated_alias("search_1")(search_code_profiles))
#: **DEPRECATED** : ``update`` is an alias for ``update_code_profile``. Exists
#: only for backward compatibility - **do not use** - use ``update_code_profile`` instead.
update = register_command(
    extending=("configurationadmin", "v1", "profile"),
    module=argus_cli_module,
    alias="update"
)(deprecated_alias("update")(update_code_profile))