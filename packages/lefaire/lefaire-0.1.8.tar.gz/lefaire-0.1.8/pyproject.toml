[tool.poetry]
name = "lefaire"
version = "0.1.8"
description = ""
authors = ["okcedric <contact@okcedric.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
typer = "^0.15.1"
colorama = "^0.4.6"
mariadb = "^1.1.11"
packaging = "^24.2"
python-dotenv = "^1.0.1"

[tool.poetry.scripts]
lefaire = "lefaire.main:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
