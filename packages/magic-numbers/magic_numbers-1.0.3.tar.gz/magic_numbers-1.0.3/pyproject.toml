[project]
name = "magic-numbers"
version = "1.0.3"
description = "magically import magic number constants"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = ["magic_numbers"]
