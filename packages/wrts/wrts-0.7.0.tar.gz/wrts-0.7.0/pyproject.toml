[tool.poetry]
name = "wrts"
version = "0.7.0"
description = "A small lil library for using the WRTS API "
authors = ["BadPythonCoder <xsare435@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10" # The lie becomes a little less extreme......
requests = "^2.31.0"

[tool.poetry.scripts]
test = "example:start"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
