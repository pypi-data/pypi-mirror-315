[project]
name = "pyproteinsext"
version = "3.1.5"
description = "'Extending pyproteins for bioinformatics tools&services'"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pyproteins>=2.1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
