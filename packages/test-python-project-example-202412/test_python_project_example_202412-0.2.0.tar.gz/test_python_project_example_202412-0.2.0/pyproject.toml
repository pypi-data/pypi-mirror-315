[project]
name = "test-python-project-example-202412"
version = "0.2.0"
description = "Test python project example"

[build-system]
requires = ["setuptools>=51", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages]
find = {where = ["src"]}
